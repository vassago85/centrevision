{{-- Shown beside a plate the platform read from the JPEG, not the camera. --}}
<span
    title="Read from the photo. The camera did not send a plate."
    data-test="read-from-photo"
    {{ $attributes->class('inline-flex items-center rounded-full bg-surface-2 px-2 py-0.5 text-[10px] font-semibold uppercase tracking-[0.08em] text-ink-2') }}
>From photo</span>
